<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeWidth="1dp"
    app:strokeColor="#DCDCDC"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Waste Name -->
        <TextView
            android:id="@+id/tvWasteName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Waste Name"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="@android:color/black" />

        <!-- Waste Type -->
        <TextView
            android:id="@+id/tvWasteType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Waste Type: Used Oil"
            android:textSize="14sp"
            android:layout_marginTop="4dp"
            android:textColor="#555555" />

        <!-- Quantity -->
        <TextView
            android:id="@+id/tvQuantity"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Quantity: 38 liters"
            android:textSize="14sp"
            android:layout_marginTop="2dp"
            android:textColor="#555555" />

        <!-- Storage Location -->
        <TextView
            android:id="@+id/tvStorageLocation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Storage Location: Warehouse A"
            android:textSize="14sp"
            android:layout_marginTop="2dp"
            android:textColor="#555555" />

        <!-- Date Generated -->
        <TextView
            android:id="@+id/tvDateGenerated"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Date Generated: Nov 27, 2025"
            android:textSize="14sp"
            android:layout_marginTop="2dp"
            android:textColor="#555555" />

        <!-- Status -->
        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Status: Draft"
            android:textStyle="bold"
            android:textSize="14sp"
            android:textColor="#FFA000"
            android:layout_marginTop="10dp" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
